import React from "react";

// Animate slide in when clicked

class WorkExpCard extends React.Component {

  render() {

    //  const { client, company, project, desc, link } = this.props.workExperience;

    return (
      <li className="work-exp-card">
        {/* <h3 className="client">{this.workExperience.project1.client}</h3> */}
        {/* <h3 className="company">{company}</h3>
        <h4 className="project">{project}</h4>
        <p className="desc">{desc}</p>
        <a href="" className="link">
          {link} */}
        {/* </a> */}
      </li>
    );
  }
}
export default WorkExpCard;
